// Shared page-width tokens so every page lines up with the homepage.
// Standard pages (home, about, methodology, speakers, subscriptions, …) use
// `pageWidth`; only transcript pages, which need room for the side-by-side
// player/transcript layout, use `widePageWidth`.
export const pageWidth = "max-w-5xl";
export const widePageWidth = "max-w-7xl";
